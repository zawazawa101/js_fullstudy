<!--pages/cart/cart.wxml-->
<view class="main">
  <view>
    <view class="cart-box">
      <view class="cart-list" wx:for="{{carts}}" wx:key="index">
         <icon bindtap="changeSelected" class="cart-pro-select" type="success" size="23" color="{{isSelected>0?'red':'black'}}"></icon>
         <navigator class="cart-pro-goods" url="">
          <image class="cart-thumb" src="{{item.image}}" />
         </navigator>
         <text class="cart-pro-name">{{item.title}}</text>
         <text class="cart-pro-price">{{item.price}}</text>

         <view class="cart-count-box">
           <text class="cart-count-down">-</text>
           <text class="cart-count-num">{{item.num}}</text>
           <text class="cart-count-add">+</text>
         </view>
         <text class="cart-del">x</text>
      </view>
    </view>

    <view class="cart-footer">
      <icon class="total-select" type="success" size="23" color="white"></icon>
      <text>全选</text>
      <text class="cart-total-price">￥{{totalPrice}}</text>
      <view class="return_pic">
        <image src="../../image/右箭头 (1).png"/>
      </view>
    </view>
  </view>
</view>
